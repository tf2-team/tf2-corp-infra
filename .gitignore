# Terraform
.terraform/
# .terraform.lock.hcl is not ignored to commit provider selections intentionally
backend.hcl
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.d/
crash.log
crash.*.log

# Override files (dùng cho local dev, không thuộc về repo chung)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI output / plan files
*.tfplan
*.plan
tfplan

# OS & editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# Python (load-generator, scripts)
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Terraform-built Lambda packages
*.zip

# Node (nếu có script helper)
node_modules/

# Log files
*.log

.antigravity/

client-vpn-pki

# Lambda ZIP artifacts - committed to avoid CI build step
!modules/audit-pipeline/lambda/alert_lambda.zip
!modules/audit-pipeline/lambda/parse_lambda.zip
!modules/runtime-security-alerting/lambda/audit_classifier.zip
